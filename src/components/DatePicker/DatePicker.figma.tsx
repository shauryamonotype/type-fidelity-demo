import figma from '@figma/code-connect'
import { DatePicker } from './DatePicker'

/**
 * DatePicker — Figma Code Connect
 *
 * Map to the DatePicker component in the Antiqua Design System Figma file once
 * the component is added there. Run `npx figma connect publish --token <token>`
 * to publish.
 *
 * figma.connect(DatePicker, 'https://figma.com/design/FILEID/...')
 */

export default figma
